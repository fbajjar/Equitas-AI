This is the local version of our application.

The demo app is available in AI Studio: https://ai.studio/apps/drive/1ZT2AtKgEudRp03bIZMaQqEHW38gir1ku

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
